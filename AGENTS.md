# AGENTS.md

本文件适用于仓库根目录 `F:\zhangbin_codex\b站数据看板1.0版本` 及其全部子目录。

## 1. 项目概览

- 项目类型：`Vite + React 19 + TypeScript` 单页数据看板。
- 核心用途：基于 B 站近期稿件 CSV 数据，展示播放、留存、互动、涨粉等指标。
- 图表方案：`echarts` + `echarts-for-react`。
- 动效方案：`framer-motion`。
- CSV 解析：`papaparse`。

## 2. 目录与职责

- `src/main.tsx`：应用入口，仅负责挂载 `App` 和全局样式引入。
- `src/App.tsx`：页面主界面、状态管理、图表配置、筛选交互与展示逻辑。
- `src/data.ts`：CSV 字段定义、数据解析、格式化、聚合统计、排序与环比计算。
- `src/styles.css`：全局视觉样式、布局、响应式规则和组件外观。
- `public/data/recent-videos.csv`：当前默认数据源。
- `public/data/recent-videos-prev.csv`：上一版对比数据源，用于计算变化。
- `近期稿件对比.csv`：原始参考数据，不要随意改名或删除，除非任务明确要求。
- `dist/`：构建产物目录，通常不手改。

## 3. 工作方式

- 修改代码前，先阅读 `README.md`、`package.json`、`src/App.tsx`、`src/data.ts`。
- 优先做最小必要改动，不因为顺手而重构无关代码。
- 若需求只涉及视觉表现，优先改 `src/styles.css`；不要把纯样式问题扩散到数据层。
- 若需求只涉及字段计算或 CSV 兼容，优先改 `src/data.ts`；不要无故改动图表结构。
- 若需求涉及页面编排、卡片、图表、筛选或联动，再改 `src/App.tsx`。

## 4. 前端约定

- 保持现有“深色数据舱 + B站粉蓝点缀”的视觉方向，避免引入不协调的新主题。
- 新增 UI 时，优先复用现有配色变量和玻璃拟态风格，不要突然切成朴素后台风。
- 该项目是单页看板，除非任务明确要求，不要擅自引入路由、状态库或新的 UI 框架。
- 已使用 TypeScript，新增逻辑应补足类型，不要轻易退化成大量 `any`。
- 仅在必要时增加注释，注释应解释原因或约束，不要写低信息量注释。

## 5. 数据与字段约定

- `src/data.ts` 中 `RawVideoRow` 的字段名直接对应 CSV 表头；修改表头兼容逻辑时要同步检查解析函数。
- 百分比字段当前按字符串解析后统一转小数，展示时再格式化为百分比。
- 视频对比依赖标题匹配：`computeDeltas()` 当前按 `title` 对齐新旧数据，修改时注意回归风险。
- 若新增指标，优先沿用现有 `normalizeRow -> buildSummary -> App 展示` 这条链路。
- 若调整数据源文件名或路径，必须同步更新 `loadDefaultVideos()` / `loadPrevVideos()`。

## 6. 常用命令

- 安装依赖：`npm install`
- 本地开发：`npm run dev`
- 生产构建：`npm run build`
- 本地预览：`npm run preview`

## 7. 修改后验证

- 功能改动后至少执行一次：`npm run build`
- 若改了图表、布局或交互，建议再执行 `npm run dev` 人工检查：
  - 页面是否正常加载
  - CSV 是否成功读取
  - 图表是否渲染
  - 表格筛选和选中联动是否正常
  - 移动端断点下是否没有明显布局错位
- 若改了 `src/data.ts` 中的解析逻辑，要重点检查：
  - 百分比字段是否仍按预期显示
  - 环比增减是否正确
  - 空数据或缺失上一版 CSV 时是否能安全降级

## 8. 禁止事项

- 不要提交密钥、Token、`.env` 等敏感信息。
- 不要随意删除 `public/data` 下的数据文件。
- 不要无需求升级依赖版本或替换构建工具。
- 不要修改 `dist/` 来代替修改源码。

## 9. 交付说明建议

完成任务后，说明应至少包含：

- 改了哪些文件
- 为什么这样改
- 如何验证
- 是否存在未验证风险或需要用户继续确认的点
